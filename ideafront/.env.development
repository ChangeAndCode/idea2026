# Solo aplica con `npm run dev`.
# Vacío → el front llama a /api… y Vite proxifica a http://localhost:4000 (vite.config.js).
VITE_API_URL=

# Para usar el backend publicado en Render desde tu máquina, pon la URL sin barra final y reinicia Vite:
# VITE_API_URL=https://tu-servicio.onrender.com
